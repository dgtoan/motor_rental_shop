import 'package:flutter/material.dart';

class ListRevenueView extends StatelessWidget {
  const ListRevenueView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}