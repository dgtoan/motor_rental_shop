import 'package:flutter/material.dart';

class RevenueFilterView extends StatelessWidget {
  const RevenueFilterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}