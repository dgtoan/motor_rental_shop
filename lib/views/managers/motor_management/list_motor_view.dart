import 'package:flutter/material.dart';

class ListMotorView extends StatelessWidget {
  const ListMotorView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}