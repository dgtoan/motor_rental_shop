import 'package:flutter/material.dart';

class ListContractView extends StatelessWidget {
  const ListContractView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}