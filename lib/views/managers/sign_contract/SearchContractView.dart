import 'package:flutter/material.dart';

class SearchContractView extends StatelessWidget {
  const SearchContractView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}